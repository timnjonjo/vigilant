package com.turing.vigilant.graph;

/**
 * The identity-overlap edge types materialised in this slice. Payment and
 * identity-attribute overlaps (spec section 5) are stubbed for now.
 */
public enum SharedAttributeType {
    DEVICE,
    IP_SUBNET
}
